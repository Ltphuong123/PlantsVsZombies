// using System.Collections;
// using System.Collections.Generic;
// using UnityEngine;

// public class Food : GameUnit
// {
    
// }

// //  SimplePool.Spawn<Effect>(PoolType.EffectSmoke, pos.position, Quaternion.identity).Play();
// UIManager.Instance.OpenUI<CanvasFail>().SetBestScore(score);
//         if (UIManager.Instance != null && UIManager.Instance.IsUIoLaded<CanvasGamePlay>())
//         {
//             UIManager.Instance.GetUI<CanvasGamePlay>().UpdateCoin(coinCount);
//         }